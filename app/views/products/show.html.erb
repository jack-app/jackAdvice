<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>About:</strong>
  <%= @product.about %>
</p>

<p>
  <strong>Need people:</strong>
  <%= @product.need_people %>
</p>

<p>
  <strong>Goal:</strong>
  <%= @product.goal %>
</p>
<tbody>
<% @product.comments.each do |comment| %>
  <tr>
    <td><%=comment.commenter %></td>
    <td><%= comment.content %><br></td>
  </tr>
<% end %>
</tbody>
<%=render 'comments_form'%>
<%=link_to '更新', product_path(@product)%>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
